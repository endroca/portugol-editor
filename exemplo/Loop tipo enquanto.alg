algoritmo "Loop - tipo enquanto"

var loop : inteiro

inicio
    loop <- 1
    enquanto loop <= 10 faca
        escreval(loop)
        espere(1000)
        loop <- loop + 1
    fimenquanto
fimalgoritmo